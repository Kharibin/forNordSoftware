'use strict';

var _ = require('../');

test('script line shape', function () {
  expect(new _.ScriptLine(5, 'foobar', true)).toMatchSnapshot();
});

test('script line to provide default highlight', function () {
  expect(new _.ScriptLine(5, 'foobar')).toMatchSnapshot();
});